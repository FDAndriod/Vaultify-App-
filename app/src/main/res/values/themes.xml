<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Material Components (AppCompat descendant). Platform Theme.Material breaks UMP / other
         libraries that show AppCompat AlertDialog: "You need to use a Theme.AppCompat theme". -->
    <style name="Theme.Vaultify" parent="Theme.Material3.Light.NoActionBar" />
</resources>